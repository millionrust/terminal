[package]
name = "termirust-protocol"
version = "0.1.0"
edition = "2024"
description = "Shared TermiRust mobile vault and protocol schema"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
