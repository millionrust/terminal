# E02.9 Project worktree operations evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `d83c0ce` `refactor(project): centralize worktree operations`

## Result

`ProjectCoordinator` is now the only GPUI boundary for managed-worktree inspection,
creation, and verification. The existing review UI, background execution, generation
fences, intent persistence, ProjectRepository registration, cancellation, and recovery
behavior remain with their prior owners.

## Delivered

- Added closed typed requests for inspection and plan-based operations.
- Routed all GPUI `GitRunner` inspect, create, and verify calls through
  `ProjectCoordinator`.
- Preserved exact project and managed paths, worktree and child-project IDs, launch
  draft, worktree plan, and shared cancellation tokens.
- Added an injectable worktree worker port with operation-specific error passthrough.
- Added a source boundary preventing any other GPUI module from constructing
  `GitRunner` directly.
- Kept repository registration after successful verification and left all recovery
  semantics unchanged.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust project_coordinator --locked` | PASS, 7 tests |
| `cargo test -p termirust worktree --locked` | PASS, 20 tests |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 510 passed desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed dependency and future-incompatibility
warnings. Free disk space remained at 21 GiB after verification.

## Next boundary

The Controller device view still constructs its device service and applies device
mutation operations directly. The next finite leaf is to extract typed device mutation
operations behind a non-GPUI `ControllerCoordinator` while preserving repository,
capability, channel, and presentation behavior.
