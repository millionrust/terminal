# E02.8 Project launch coordinator evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `3e23de6` `refactor(project): centralize session launch planning`

## Result

A non-GPUI `ProjectCoordinator` now owns New Session review validation and exact domain
launch resolution. The sheet remains the owner of phases, localization, persistence,
pane registration, and the handoff to `SessionCoordinator`.

## Delivered

- Added closed review input, reviewed launch, review-error, and resolution types.
- Preserved missing-preset, unavailable-store, stale-revision, missing-project, and
  missing-preset decision precedence.
- Preserved exact project/preset clones, generated Session ID, bounded PATH snapshot,
  and platform home at the domain resolver boundary.
- Added an injectable resolver port and a real system-resolver test.
- Kept filesystem/executable validation on the existing background executor.
- Kept generation fencing before resolved results can mutate the sheet.
- Added a source boundary preventing direct GPUI calls to domain `resolve_launch`.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust project_coordinator --locked` | PASS, 6 tests |
| `cargo test -p termirust new_session --locked` | PASS, 5 tests including reviewed durable launch/input/stop flow |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 509 passed desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed duplicate-dependency and future-
incompatibility warnings. Free disk space remained at 21 GiB after verification.

## Next boundary

Managed worktree inspection, creation, verification, registration, cancellation, and
recovery still call their service directly from a GPUI module. The next finite leaf is
to extract only the typed worktree operation boundary behind ProjectCoordinator while
leaving the review sheet and Project store presentation in GPUI.
