# E02.18 Canvas placement coordinator completion evidence

- Status: complete
- Implementation commit: `7d4ae53` `refactor(canvas): centralize node placement`
- Scope: the app-owned, non-GPUI `CanvasCoordinator` owns deterministic non-overlapping
  placement arithmetic. Every live terminal/agent/note/group, layout conversion, workspace
  creation, and restore path passes the same coordinator; canvas state still constructs nodes.

## Characterization

- Empty canvases preserve centered origin placement.
- Occupied canvases preserve ring traversal order and deterministic repeatability.
- Gutter intersections reject otherwise touching candidates.
- Bounded search preserves the edge-count fallback.
- Node dimensions, identity, titles, z-order, saved viewport, and restore behavior remain exact.

## Verification

- `cargo fmt --all -- --check`: PASS
- `cargo test -p termirust canvas_coordinator --locked`: PASS, 15 tests
- `cargo test -p termirust placement_is --locked`: PASS, 1 test
- `cargo test -p termirust runtime_saved_round_trip --locked`: PASS, 1 test
- `cargo test -p termirust e2e_canvas --locked`: PASS, 7 tests
- `python3 scripts/clippy-changed.py`: PASS
- `./scripts/verify-rust.sh workspace`: PASS, 534 desktop tests passed, 3 ignored;
  workspace tests and dependency policy passed
- `git diff --check`: PASS

## Preserved boundaries

No drag, resize, minimap, fit/reveal, layout history, group membership, node identity,
rendering, persistence schema, or interaction behavior changed.
