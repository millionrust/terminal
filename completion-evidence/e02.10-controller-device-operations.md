# E02.10 Controller device operations evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `449b501` `refactor(controller): centralize device mutations`

## Result

A non-GPUI `ControllerCoordinator` is now the only GPUI boundary for paired-device
rename, input-capability mutation, and revocation service calls. RemoteDevicesState
retains repository refresh, pairing notices, and visible presentation state.

## Delivered

- Added a closed typed device-mutation request and operation enum.
- Routed all GPUI device-service rename, set-capabilities, and revoke calls through
  `ControllerCoordinator`.
- Moved the exact existing input-capability enable/disable projection into the
  coordinator.
- Preserved exact repository, device IDs, display names, capability sets,
  `NoControllerChannels`, flattened UI failure behavior, refresh order, and messages.
- Added an injectable mutation port with exact forwarding and failure tests.
- Added a real temporary-repository adapter test covering rename, capability update,
  and revoke.
- Added source boundaries preventing GPUI dependencies in the coordinator and direct
  device-service construction in other GPUI modules.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust controller_coordinator --locked` | PASS, 5 tests |
| `cargo test -p termirust remote_devices --locked` | PASS, 7 tests |
| `cargo test -p termirust controller_devices --locked` | PASS, 1 test |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 515 passed desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed dependency and future-incompatibility
warnings. Free disk space remained at 21 GiB after verification.

## Next boundary

RemoteDevicesState still constructs, starts, and stops the Controller listener process
directly. The next finite leaf is to route listener runtime operations through
ControllerCoordinator while leaving route policy persistence, pairing, polling, and UI
state transitions unchanged.
