# E02.11 Controller listener operations evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `15d6e1c` `refactor(controller): centralize listener runtime operations`

## Result

The app-owned non-GPUI `ControllerCoordinator` is now the only GPUI boundary for
starting and explicitly stopping the Controller listener runtime, including startup
restoration of a previously enabled route.

## Delivered

- Added an injectable listener-spawn port to `ControllerCoordinator`.
- Routed saved-policy startup and user-triggered listener start through the coordinator.
- Routed explicit stop and identity-reset shutdown through the coordinator.
- Threaded the one app-owned coordinator through RemoteDevicesState lifecycle methods.
- Preserved exact launch descriptor construction, typed launch errors, process type and
  Drop behavior, policy persistence/rollback, repository reload, listener states, route
  availability, and pairing cleanup.
- Added exact descriptor root/revision/policy and error-forwarding characterization.
- Added source boundaries preventing direct raw listener start/stop in other GPUI
  modules.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust controller_coordinator --locked` | PASS, 6 tests |
| `cargo test -p termirust remote_devices --locked` | PASS, 7 tests |
| `cargo test -p termirust process_event_stream --locked` | PASS, 1 test |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 516 passed desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed dependency and future-incompatibility
warnings. Free disk space remained at 21 GiB after verification.

## Next boundary

RemoteDevicesState still issues begin/confirm/reject pairing commands directly to the
listener process or SSH pairing broker. The next finite leaf is to centralize typed
pairing command dispatch while preserving offer fencing and all UI state transitions.
