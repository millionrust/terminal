# E07.2 Completion Evidence

Status: complete

Implementation commit: `d13ce6b feat(ssh): support OpenSSH certificate authentication`

## Delivered

- Added backward-compatible certificate fields across saved profiles, drafts, connection
  requests, jump chains, and restorable workspaces.
- Added one redacted authentication adapter shared by terminal, remote-exec, jump-host, and SFTP
  connections.
- Validates bounded OpenSSH user certificates for type, time, principal, and private-key binding.
- Added separate private-key and certificate editor controls and `CertificateFile` SSH-config
  import.
- Prevents fallback to raw private-key authentication and rejects unsupported mobile export.
- Added deterministic Docker CA policy and generated trusted/untrusted user certificates.

## Automated verification

- `./scripts/verify-openssh-certificate-auth.sh` - passed.
- `cargo test -p termirust ssh::tests --locked` - 14 passed.
- `cargo test -p termirust sftp::tests --locked` - 2 passed.
- Live Docker acceptance passed for direct SSH, two-hop jump SSH, SFTP, and untrusted-CA
  rejection without fallback.
- `python3 scripts/clippy-changed.py` - changed Rust lines clean.
- `cargo test -q` - 576 passed, 0 failed, 3 ignored; both integration binaries passed.
- `cargo fmt --all -- --check` and `git diff --check` - passed.

## Security boundary

The client validates local certificate shape and key binding. The OpenSSH server remains
authoritative for CA trust, revocation, source restrictions, and certificate extensions. Paths,
passphrases, and key material are excluded from authentication errors and SSH debug logs.
