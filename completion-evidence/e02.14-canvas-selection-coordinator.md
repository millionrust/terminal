# E02.14 Canvas selection coordinator completion evidence

- Status: complete
- Implementation commit: `f225c0f` `refactor(canvas): centralize adjacent selection`
- Scope: one app-owned, non-GPUI `CanvasCoordinator` now owns deterministic adjacent-node
  selection decisions. `CanvasWorkspaceState` still owns selection mutation and z-order raising;
  the GPUI keyboard path still owns reveal, pane activation, focus, persistence, and notification.

## Characterization

- Empty canvases clear stale selection.
- Initial forward and backward selection preserve directional-edge behavior.
- Existing and stale selections preserve vector order and directional fallback.
- Forward and backward selection wrap at both edges.
- Full-range `isize::MIN` and `isize::MAX` deltas are handled without arithmetic overflow.
- A source boundary prevents adjacent-selection arithmetic from drifting back into canvas UI code.

## Verification

- `cargo fmt --all -- --check`: PASS
- `cargo test -p termirust canvas_coordinator --locked`: PASS, 5 tests
- `cargo test -p termirust keyboard_selection --locked`: PASS, 1 test
- `cargo test -p termirust e2e_canvas --locked`: PASS, 7 tests
- `python3 scripts/clippy-changed.py`: PASS
- `./scripts/verify-rust.sh workspace`: PASS, 524 desktop tests passed, 3 ignored;
  workspace tests and dependency policy passed
- `git diff --check`: PASS

## Preserved boundaries

No layout geometry, node model, link, edge, drag, resize, pan, zoom, reveal, focus, process,
orchestration, persistence, rendering, or shortcut behavior changed.
