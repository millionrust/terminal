# E02.17 Canvas viewport geometry coordinator completion evidence

- Status: complete
- Implementation commit: `506c49b` `refactor(canvas): centralize viewport geometry`
- Scope: the app-owned, non-GPUI `CanvasCoordinator` owns deterministic fit-to-content and
  reveal-in-viewport calculations through closed primitive geometry contracts. Canvas adapters
  retain node rendering, transform mutation, persistence, terminal resizing, and notification.

## Characterization

- Empty content returns the exact default transform.
- Single content is centered and multi-node bounds preserve fit behavior.
- Minimum and maximum zoom clamps remain exact.
- Already visible rectangles produce no movement.
- Clipped axes move independently; oversized content centers per axis.
- Production keyboard, activity-focus, and fit actions use the app-owned coordinator.

## Verification

- `cargo fmt --all -- --check`: PASS
- `cargo test -p termirust canvas_coordinator --locked`: PASS, 13 tests
- `cargo test -p termirust keyboard_reveal --locked`: PASS, 1 test
- `cargo test -p termirust fit_transform --locked`: PASS, 1 test
- `cargo test -p termirust e2e_canvas --locked`: PASS, 7 tests
- `python3 scripts/clippy-changed.py`: PASS
- `./scripts/verify-rust.sh workspace`: PASS, 532 desktop tests passed, 3 ignored;
  workspace tests and dependency policy passed
- `git diff --check`: PASS

## Preserved boundaries

No placement, drag, resize, minimap, layout history, group membership, selection, links,
rendering, persistence, node model, viewport constants, or interaction behavior changed.
