# E05.1 mobile route identity, capability, and navigation contract

Date: 2026-08-29
Status: complete

## Delivered

- Added canonical `mobile-route-contract-v1` ownership and capability fixtures for saved
  direct-SSH Connections, paired Devices, and durable Device Sessions.
- Added pure Swift and Kotlin validated projections plus top-level Connections/Devices
  destinations and guarded terminal destinations.
- Rejected Host replay/writer claims on direct SSH, SSH credentials/tmux continuity on
  Device Sessions, terminal ownership on Device rows, duplicate capabilities, and unknown
  capabilities.
- Added host-running Swift, JVM Kotlin, and root structural verification while retaining
  all E04 rendering, interaction, lifecycle, performance, and route-isolation gates.
- Changed Android's development-only direct-SSH app label/version suffix so product-facing
  metadata no longer calls it a legacy app; the controller build remains isolated until
  E05.3 performs the reviewed merge.

## Commits

- Rust canonical contract: `1fd1d61 test(mobile): define route ownership contract`
- Swift route model: `119141b feat(mobile): add route ownership model`
- Kotlin route model: `e583072 feat(mobile): add route ownership model`

The Rust commit is on branch `test`; native repositories have no configured remotes. No
commit contains a coauthor trailer.

## Verification

- `./scripts/verify-mobile-route-contract.py` - passed.
- `./scripts/sync-terminal-conformance-fixture.sh --check` - passed.
- `./scripts/verify-ios-controller.sh --stage route-contract` - passed the route corpus,
  terminal acceptance/interaction, v1/v2 conformance, Swift 6 strict source/test typecheck,
  and localization compilation. No simulator runtime is installed.
- `./scripts/verify-android-controller.sh --stage route-contract` - passed route tests,
  terminal acceptance/interaction, v1/v2 conformance, writer/resource tests, and the
  controller no-direct-SSH isolation check.
- The full Rust workspace gate and native application build/test gates passed immediately
  before this model-only leaf and E05.1 changed no runtime Rust code.
- `git diff --check` passed in all three repositories; free disk remained 19 GB.

## Scope Notes

This leaf defines route truth but does not yet compile direct SSH into either unified
product target. E05.2 and E05.3 own those platform integrations.
