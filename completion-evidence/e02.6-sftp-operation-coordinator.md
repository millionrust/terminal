# E02.6 SFTP operation coordinator evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `28824eb` `refactor(connection): centralize SFTP worker starts`

## Result

`ConnectionCoordinator` is now the only application boundary that starts SFTP list,
upload, download, and delete workers. The GPUI application still owns Files-view state
and applies SFTP events; protocol behavior and user interaction are unchanged.

## Delivered

- Added a closed typed request for list, upload, download, and delete operations.
- Preserved workspace and operation IDs, the complete `ConnectRequest`, remote and local
  paths, and the delete-directory flag.
- Moved ownership of the shared SFTP event sender and exact known-host store into the
  coordinator.
- Added an injectable SFTP worker port with adapter tests covering every operation.
- Proved the same known-host `Arc` and event sink cross each worker boundary.
- Added source-boundary coverage preventing GPUI modules from calling low-level SFTP
  worker starts directly.
- Removed the duplicate SFTP sender field from `TermiRustApp` while retaining its event
  receiver and existing Files presentation state.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust connection_coordinator --locked` | PASS, 11 tests |
| `cargo test -p termirust sftp --locked` | PASS, 6 tests |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 498 passed desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed duplicate-dependency and future-
incompatibility warnings. Free disk space remained at 21 GiB after verification.

## Next boundary

SFTP event interpretation remains in the GPUI event loop. The next finite extraction is
the deterministic SFTP event projection: stale-operation rejection, directory results,
transfer-complete refresh decisions, and error classification. Files-view rendering and
mutation remain application-owned.
