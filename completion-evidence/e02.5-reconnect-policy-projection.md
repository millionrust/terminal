# E02.5 Reconnect policy projection evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `4f47932` `refactor(connection): centralize reconnect policy`

## Result

`ConnectionCoordinator` now owns automatic reconnect eligibility, bounded attempt
progression, deadline calculation, waiting countdown, and due classification. The GPUI
application applies explicit decisions and still owns pane replacement.

## Delivered

- Added closed disabled, user-closed, local-shell, exhausted, scheduled, ignored,
  waiting, and due decisions.
- Preserved attempt numbering and exact `Reconnecting in Ns (x/y)` status text.
- Preserved ceiling-second countdown behavior and current-setting max-attempt display.
- Added saturating deadline and countdown arithmetic at clock limits.
- Synchronized reconnect attempts and delay setting changes into the coordinator.
- Kept actual reconnect pane replacement and credential refresh unchanged.
- Added source-boundary coverage preventing timing arithmetic and status construction
  from returning to the application module.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust connection_coordinator --locked` | PASS, 10 tests |
| `cargo test -p termirust reconnect --locked` | PASS, 14 tests |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 497 desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed duplicate-dependency and future-
incompatibility warnings. Free disk space remained at 21 GiB after verification.
