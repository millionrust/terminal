# E02.4 Connection start coordinator evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `6e96c86` `refactor(connection): centralize worker starts`

## Result

One non-GPUI `ConnectionCoordinator` now owns the shared event sink, known-host store,
current SSH keepalive value, and local-versus-SSH worker routing. Every application pane
start, including restore and all reconnect paths, crosses this boundary.

## Delivered

- Added a typed local/SSH worker classification and one application start method.
- Kept existing local PTY and SSH spawn functions as low-level adapters.
- Proved local requests receive the shared event sink.
- Proved SSH requests receive the shared event sink, the exact same known-host `Arc`, and
  the current keepalive value.
- Synchronized runtime keepalive-setting changes into the coordinator.
- Added source-boundary coverage preventing other `src/ui/app` modules from calling
  low-level local or SSH worker spawn functions.
- Kept the coordinator independent of GPUI.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust connection_coordinator --locked` | PASS, 4 tests |
| `cargo test -p termirust local --locked` | PASS, 34 tests |
| `cargo test -p termirust ssh --locked` | PASS, 37 tests |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 491 desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed duplicate-dependency and future-
incompatibility warnings. Free disk space remained at 21 GiB after verification.
