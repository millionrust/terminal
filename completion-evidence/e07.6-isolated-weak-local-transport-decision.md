# E07.6 Completion Evidence

Status: complete

Decision commit: `13b757d docs: define weak and local transport boundaries`

## Result

- Plain Telnet is excluded. Its default cleartext, unauthenticated, downgradeable transport does
  not meet TermiRust's infrastructure-access contract and cannot reuse SSH trust or credentials.
- Direct serial is deferred as a separate local-device milestone. Its model, discovery,
  permissions, device settings, hotplug, and platform lifecycle do not belong in `HostProfile` or
  the SSH session runtime.
- Removed the decorative Telnet connection card, editor section, overflow action, state, and port
  input, plus the unused serial asset. The remaining dialog is truthfully named Connection
  settings and contains only the operational SSH route.
- Documented finite architecture, security, hardware, UI, and per-platform evidence prerequisites
  for any later serial milestone.

## Automated Verification

- `./scripts/verify-weak-local-transport-decision.sh` - passed.
- `cargo test -p termirust-domain ssh_access --locked -- --test-threads=1` - 4 passed.
- `cargo test -p termirust e2e_choose_protocol --locked -- --test-threads=1` - 3 passed,
  including live SSH and one-shot agent-forwarding paths.
- `python3 scripts/clippy-changed.py` - changed Rust lines clean.
- `DOCKER_CONFIG=/tmp/termirust-docker-anonymous cargo test -q -- --test-threads=1` -
  605 passed, 0 failed, 3 ignored; both integration binaries passed.
- `cargo fmt --all -- --check` and `git diff --check` - passed.

The ordinary parallel full-suite invocation encountered an existing shared-isolation-lock stall;
the deterministic single-thread rerun passed every test and left no fixture container running.
