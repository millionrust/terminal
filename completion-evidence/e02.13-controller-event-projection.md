# E02.13 Controller event projection evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `25668e1` `refactor(controller): centralize event projection`

## Result

`ControllerCoordinator` now owns raw SSH-broker/listener polling and deterministic
listener event projection. RemoteDevicesState applies closed projections to UI state
and retains repository refresh, timers, notices, and presentation.

## Delivered

- Routed SSH prompt polling, listener event drain, and process liveness checks through
  `ControllerCoordinator`.
- Added closed event projections for offer, SAS-ready, complete, and failed outcomes.
- Added closed projection errors for unexpected Ready and offer mismatch.
- Preserved exact offer-ID fencing, offer/SAS fields, completion refresh, event order,
  process-exit handling, and existing failure-code to UI-state mapping.
- Characterized every event variant, every failure bucket, stale event rejection, and
  ignored protocol fields.
- Added source boundaries preventing raw polling in other GPUI modules.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust controller_coordinator --locked` | PASS, 9 tests |
| `cargo test -p termirust remote_devices --locked` | PASS, 7 tests |
| `cargo test -p termirust process_event_stream --locked` | PASS, 1 test |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 519 passed desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed dependency and future-incompatibility
warnings. Free disk space remained at 21 GiB after verification.

## Next boundary

Session, Connection, Project, and Controller coordinators now have cohesive boundaries.
Canvas remains the fifth named coordinator responsibility and still combines layout,
selection, links, orchestration, process state, and presentation in one large GPUI
module. The next finite leaf will extract one deterministic Canvas behavior behind a
non-GPUI `CanvasCoordinator`.
