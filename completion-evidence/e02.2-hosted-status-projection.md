# E02.2 Hosted status projection evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `d91e18f` `refactor(session): centralize hosted status projection`

## Result

`SessionCoordinator` now owns deterministic Host status projection into durable Session
lifecycle, replay watermarks, activity observation, compatibility persistence warnings,
Dev URL availability actions, and pending stop/archive outcomes. The GPUI event loop only
locates panes, applies presentation fields, and reports explicit coordinator outcomes.

## Delivered

- Added a typed Host status input and closed pane, Dev URL, warning, and pending-archive
  projection results.
- Preserved max-only `last_sequence` and `durable_sequence` advancement.
- Preserved gap-specific Dev URL behavior and the exact terminal host-unavailable state
  set.
- Moved Session Library reconciliation behind the coordinator boundary.
- Added an activity observer port so notification recording remains injectable and the
  coordinator imports no GPUI types.
- Kept compatibility-save and activity-observation failures non-fatal and observable.
- Stopped persistence and notification effects after an authoritative reconciliation
  failure.
- Replaced duplicate archive lifecycle interpretation with a closed coordinator action.
- Added a source-boundary test preventing reconciliation from drifting back into the
  application event loop.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust session_coordinator --locked` | PASS, 9 tests |
| `cargo test -p termirust session_library --locked` | PASS, 9 tests |
| `cargo test -p termirust activity_center --locked` | PASS, 6 tests |
| `cargo test -p termirust session_sidebar --locked` | PASS, 4 tests |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 483 desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed duplicate-dependency and future-
incompatibility warnings. Free disk space remained at 21 GiB after verification.
