# E02.7 SFTP event projection evidence

- Completed: 2026-08-29
- Owner: Codex
- Canonical repository: `/Users/jacob/Projects/terminal`
- Branch: `test`
- Implementation commit: `c3ae104` `refactor(connection): centralize SFTP event projection`

## Result

`ConnectionCoordinator` now interprets every raw SFTP worker event and returns a closed
Files presentation decision. The GPUI application reads the current operation ID and
applies accepted decisions to its existing workspace browser state.

## Delivered

- Added exact workspace/operation event identity for every SFTP event variant.
- Added closed ignore, directory result, completion, and failure projections.
- Fenced every event against the workspace's current pending operation ID.
- Prevented late upload/delete completions from replacing a newer operation with an
  unintended directory refresh.
- Preserved canonical path, entry order and metadata, transfer paths, and error bytes.
- Preserved exact success status text and upload/delete-only refresh behavior.
- Kept loading, selection, entry, status/error, rendering, and follow-up start mutation
  in the GPUI application.
- Added a source boundary preventing GPUI modules from matching raw SFTP event variants.

## Verification

| Command | Result |
| --- | --- |
| `cargo fmt --all -- --check` | PASS |
| `cargo test -p termirust connection_coordinator --locked` | PASS, 16 tests |
| `cargo test -p termirust sftp --locked` | PASS, 8 matching tests including Docker and Files UI flows |
| `python3 scripts/clippy-changed.py` | PASS, changed lines clean |
| `./scripts/verify-rust.sh workspace` | PASS, including 503 passed desktop tests, 3 ignored, all workspace targets, docs, advisories, bans, licenses, and sources |
| `git diff --check` | PASS |

The full gate reports only existing allowed duplicate-dependency and future-
incompatibility warnings. Free disk space remained at 21 GiB after verification.

## Next boundary

Connection-related tunnel startup and lifecycle still live outside the coordinator.
The next leaf should first characterize tunnel ownership and event flow, then extract
only its worker-start boundary if it is independent of pane presentation state.
